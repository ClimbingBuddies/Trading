# OPS-007 — Resolve historical Market run and backlog

## Independent review — 19 August 2026, 12:02 Australia/Perth

- **Task:** `OPS-007 — Resolve historical Market run and backlog`
- **Project-plan status at review start:** `IN REVIEW`
- **Decision:** `PASS WITH ADVICE`
- **Final project-plan status:** `DONE`
- **Next promoted task:** `SEC-001 — Define public/private Market Assessment access`

## Definition of Done checks

| Requirement | Verdict | Independent evidence |
|---|---|---|
| Legacy test run is deliberately archived, superseded, or finalised | **VERIFIED** | Live run `1bebc82b-5727-4d9a-ac86-e1ca32c07ed7` is `succeeded`, 30 requested / 30 completed, with 30 assessments across 30 instruments and 30 evidence rows. `completed_at` is the administrative reconciliation timestamp `2026-08-19 03:32:59.855999+00`, and the persisted note states finalisation was derived from existing assessment rows without replay or assessment/evidence mutation. |
| Historical orphan backlog is terminally resolved without replaying it as current work | **VERIFIED** | Live queue IDs 1–7, dated 3–11 August 2026, are terminal `failed` with `attempt_count = 0`, `started_at = null`, `gpt_run_id = null`, populated `processed_at`, and an explicit OPS-007 supersession message stating no GPT run was created. The live claim function selects only `pending` or `ready_for_analysis`; none of these rows remains claimable. |
| Existing historical assessment/evidence data and schedule history are preserved | **VERIFIED** | Live totals are 3 runs, 90 assessments, 158 evidence rows, 9 queue rows, and 7 schedule-log rows. The seven legacy schedule logs remain present with their original `triggered` status. The GitHub migration contains no delete and updates only lifecycle/control fields. |
| Already-correct later executions remain intact | **VERIFIED** | The 14 August test run remains `succeeded` at 30/30 with 60 evidence rows and linked queue 8; the 18 August unattended run remains `succeeded` at 30/30 with 68 evidence rows and linked queue 9. |
| Cleanup is bounded and idempotent | **VERIFIED** | The migration selectors now match zero stale running test runs and zero claimable pre-14-August orphan queues. Duplicate groups are zero for both run/instrument assessments and assessment/source/evidence rows. |
| Retired backlog ownership is not still active | **VERIFIED** | Live `cron.job` contains no active job whose name or command references Market Assessment. |
| Cleanup migration exists in source and was applied live | **VERIFIED** | GitHub contains `supabase/migrations/20260819035000_resolve_legacy_market_backlog.sql`; live migration history contains `resolve_legacy_market_backlog` and the applied statements match the source logic. |
| Canonical current-work rule excludes historical replay | **VERIFIED** | `automation/daily-market-assessment.md` requires normal production work to use the current New York assessment date and explicitly states that stale historical queue/run records must not be replayed as current work. |

## Primary evidence inspected

### GitHub

- `automation/project-plan-auditor.md`
- `documentation/project-plan.md`
- `automation/daily-market-assessment.md`
- `supabase/migrations/20260819035000_resolve_legacy_market_backlog.sql`
- `supabase/migrations/20260812_phase2_assessment_queue_lifecycle.sql`
- `documentation/operations/market-assessment-legacy-backlog-resolution.md`
- `documentation/project-audits/OPS-007.md` was checked and did not previously exist.

### Supabase

Project `glvbqcplgjdfgjyknzsa` was queried directly for:

- every Market run with assessment, instrument, evidence, and linked-queue counts;
- every Market queue row and all seven historical schedule-log rows;
- stale/claimable selector counts;
- duplicate assessment and evidence groups;
- active Market Assessment cron jobs;
- live claim/finalize queue function definitions;
- applied migration history and statements.

### Vercel / production / browser

Not required for this Definition of Done. OPS-007 concerns persisted historical run and queue lifecycle, so the authoritative primary evidence is GitHub source plus live Supabase state.

## Decision

**PASS WITH ADVICE**

Every material Definition of Done requirement is independently verified. The legacy populated test run now has a truthful terminal lifecycle derived from persisted rows, and the seven obsolete queue requests are terminally closed without attempts, run creation, or replay. Historical assessment, evidence, queue, and schedule-log records remain available.

## Non-blocking advice

The seven administratively superseded queues use the existing terminal `failed` status and distinguish their meaning through `error_message`. In a future queue-model hardening task, consider adding a structured resolution reason or an explicit supported `superseded` terminal state so reporting can separate administrative retirement from genuine attempted execution failures without parsing text. This is not required by OPS-007 and does not block completion.
