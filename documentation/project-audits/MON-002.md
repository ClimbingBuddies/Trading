# MON-002 Project Audit

## Review — 23 Aug 2026 13:39 AWST

**Task:** MON-002 — Activate watchlists  
**Project-plan status at review start:** IN REVIEW  
**Reviewed implementation/documentation state:** `154040b4a20152f380d0921878f8e4cecdcde1b5`  
**Controller handoff commit:** `d1f8dd74c53dc4b6891f7788aab6dc90528e45ee`  
**Decision:** BLOCKED

### Definition of Done

Definition of Done: **Real user-owned lists can be maintained securely.**

| Check | Verdict | Independent evidence |
|---|---|---|
| Mandatory user ownership is live | VERIFIED | Live Supabase shows `watchlists.owner_user_id` is `NOT NULL`, there are zero ownerless rows, and the existing FK remains tied to `auth.users(id)`. |
| Anonymous/public table access is removed and authenticated grants are minimal | VERIFIED | Live grants show no `anon` privileges on `watchlists`; `authenticated` has only `SELECT, INSERT, UPDATE, DELETE`. The same migration contract applies to `watchlist_items`. |
| Owner-scoped RLS is live for list and item CRUD | VERIFIED | Live `pg_policies` shows four policies on `watchlists` and four on `watchlist_items`, all scoped to `authenticated`, using `(select auth.uid())`, rejecting `is_anonymous = true`, and enforcing parent ownership for item rows. |
| Cross-user isolation and ownership-forgery protection work | VERIFIED | Auditor-run rollback-only users A/B matrix independently passed: A could maintain A data; B could not see or mutate A data; A could not forge B ownership or transfer ownership; authenticated-anonymous access was denied. |
| Membership, notes/reorder and duplicate protection work | VERIFIED | Auditor matrix inserted items, updated notes/sort order, and confirmed the `(watchlist_id, instrument_id)` primary key rejects duplicate membership. |
| Default-list integrity works | VERIFIED | Live partial unique index `watchlists_one_default_per_owner_idx` exists; auditor matrix verified first-list default behaviour and owner-checked `set_watchlist_default` switching with exactly one default. The RPC is security-invoker, has an empty search path, denies `anon`, and permits `authenticated`/`service_role`. |
| Auth-user cascade is deliberate and works | VERIFIED | Auditor matrix deleted temporary Auth user A and independently verified A's watchlists/items cascaded while B's data remained. The whole matrix then rolled back. |
| Audit tests left no production test data | VERIFIED | Fresh live state after rollback is 0 Auth users, 0 watchlists and 0 watchlist items. |
| New watchlist implementation has no post-review application drift | VERIFIED | GitHub comparison `154040b4a20152f380d0921878f8e4cecdcde1b5...d1f8dd74c53dc4b6891f7788aab6dc90528e45ee` shows only `documentation/project-plan.md` changed after the reviewed state. |
| Browser implementation uses only public Supabase configuration | VERIFIED | `lib/supabase-browser.ts` uses only `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, with persistent browser sessions; repository search found no `SERVICE_ROLE` frontend usage. |
| Watchlist route/UI is implemented in source | VERIFIED | `app/watchlists/page.tsx`, `components/WatchlistsClient.tsx`, `components/WatchlistsClient.module.css` and the `/watchlists` AppNav entry are present. Source includes email OTP/magic-link sign-in, list CRUD, instrument add/remove, notes, reorder, default selection and sign-out. |
| Existing signed-out public route remains healthy | VERIFIED | Production `/markets` returned HTTP 200 during this audit; Vercel production error/fatal logs for the preceding hour were empty. |
| Reviewed frontend builds and `/watchlists` is live in production | UNVERIFIED | Vercel has created no deployment newer than `dpl_DyULS4PDB5cfhzVhVUJBLzm65vZY` on commit `c420a440343934b50735b7757162edd48a7f7724`. Production `/watchlists` currently returns HTTP 404. GitHub Vercel status for both the reviewed commit and handoff commit is `failure` with the `build-rate-limit` target. |
| Email-auth redirect/session return works in the deployed application | UNVERIFIED | The source requests `emailRedirectTo = <origin>/watchlists` and enables URL session detection, but the route is not deployed and the project's production redirect acceptance/user-return flow therefore cannot be exercised. |
| Authenticated browser CRUD works end-to-end in production | UNVERIFIED | Database behaviour is independently proven, but there is no deployed `/watchlists` frontend and no production Auth user with which to exercise the real browser path. |

### Primary evidence inspected

#### GitHub

- `automation/project-plan-auditor.md` — specification v1.1, retrieved fresh at audit start.
- `documentation/project-plan.md` — retrieved fresh at audit start; MON-002 was the only `IN REVIEW` item.
- `documentation/project-audits/MON-002.md` — did not previously exist (404), so this is the first MON-002 review.
- `documentation/watchlist-activation.md` — implementation/deployment contract and activation gate.
- `lib/supabase-browser.ts` — separate persistent publishable-key browser client.
- `app/watchlists/page.tsx` — watchlist route entry point.
- `components/WatchlistsClient.tsx` — authenticated watchlist UI and CRUD flow.
- `components/AppNav.tsx` — `/watchlists` primary navigation entry.
- `supabase/migrations/20260823132000_activate_secure_user_watchlists.sql` — ownership, grants, RLS, triggers and default RPC.
- `supabase/migrations/20260823133500_optimize_watchlist_rls_auth_initplans.sql` — optimized RLS/JWT expressions.
- GitHub comparison `154040b4a20152f380d0921878f8e4cecdcde1b5...d1f8dd74c53dc4b6891f7788aab6dc90528e45ee` — only project-plan controller documentation changed.
- Vercel commit status for both `154040b4...` and `d1f8dd74...` — `failure` targeting the Hobby `build-rate-limit` condition.

#### Supabase

Project: `glvbqcplgjdfgjyknzsa`.

Independently verified live state:

- 0 Auth users, 0 watchlists, 0 watchlist items and 0 ownerless watchlists after audit rollback;
- `owner_user_id` is mandatory;
- four RLS policies on each watchlist table;
- no `anon` watchlist grants;
- authenticated CRUD-only table grants;
- one-default-per-owner partial unique index;
- both expected watchlist triggers;
- `set_watchlist_default(uuid)` is security-invoker with empty search path, no anon execute, authenticated/service execute;
- security adviser has no watchlist finding;
- performance adviser has no watchlist `auth_rls_initplan` finding.

The Auditor independently executed a rollback-only two-user matrix. The first harness attempt aborted on the expected ownership-transfer RLS rejection because the harness had not caught that expected exception; it was not counted as a pass. The corrected matrix then completed and returned `MON-002 independent rollback security matrix passed`. No audit test data was retained.

#### Vercel / production

- Current latest actual production deployment: `dpl_DyULS4PDB5cfhzVhVUJBLzm65vZY`, READY, Git commit `c420a440343934b50735b7757162edd48a7f7724`.
- No newer deployment exists for the reviewed frontend implementation.
- Production `/watchlists`: HTTP 404.
- Production `/markets`: HTTP 200.
- Production error/fatal runtime logs in the preceding hour: none.
- Auditor attempted the available deployment action. The connector advertised it as no-argument, but the backend rejected the call because hidden required fields (`target`, `name`, `files`) were not supplied. Re-discovery still exposes only the same no-argument schema, so this connector cannot safely target the reviewed commit from this run.
- GitHub's Vercel status independently reports `build-rate-limit` for the reviewed and handoff commits.

### Decision

**BLOCKED.** The database/security implementation is independently verified and no Builder remediation defect has been established. However, the Definition of Done requires a real user-owned watchlist flow, and that cannot be proven while the reviewed frontend is not deployed. Production currently returns 404 for `/watchlists`, the Vercel Git check remains blocked by the Hobby build-rate limit, and the available deployment connector cannot issue a valid commit-targeted deployment in this run.

This is therefore an external deployment/access blocker under the Auditor specification, not REWORK. The Builder must not change the implementation merely to work around it.

### Required unblock condition

Once Vercel permits a build or a supported exact reviewed-state deployment path is available:

1. deploy the unchanged reviewed application state (no implementation edits);
2. verify the palette/prebuild, Next.js and TypeScript build reaches a terminal READY result;
3. verify production `/watchlists` is live and signed-out behaviour is correct;
4. verify the production email magic-link redirect returns to `/watchlists` with a permanent authenticated session;
5. exercise real authenticated browser watchlist create/update/default/item/reorder/delete flow;
6. confirm public signed-out routes remain healthy and no privileged browser credential is introduced;
7. re-run the MON-002 Auditor from fresh GitHub/project state.

### Final project-plan status

- MON-002 → **BLOCKED**.
- MON-003 and all later items remain **PLANNED**.
- No task is promoted during this audit.

---

## Re-review — 24 Aug 2026 08:39 AWST

**Task:** MON-002 — Activate watchlists  
**Project-plan status at review start:** IN REVIEW  
**Reviewed application state:** `154040b4a20152f380d0921878f8e4cecdcde1b5`  
**Production deployment:** `dpl_A5RqoAKgKfHy8Tvqdhh6yvuTEcJW` on `92aa5d2684f7f6439103b9d9554dd2a7ffa84f0b`  
**Decision:** REWORK

### Re-review findings

| Check | Verdict | Independent evidence |
|---|---|---|
| Reviewed implementation is the application code deployed | VERIFIED | GitHub comparison from reviewed state `154040b4...` to deployed `92aa5d2...` shows only `documentation/project-audits/MON-002.md` and `documentation/project-plan.md` changed; there is no application/source/schema drift. |
| Production build is healthy | VERIFIED | Vercel deployment `dpl_A5R...` is READY. Build logs show palette compliance passed for 18 files, Next.js compiled successfully, TypeScript completed successfully, and `/watchlists` was generated. |
| Production `/watchlists` is live | VERIFIED | Production fetch returned HTTP 200 and `x-matched-path: /watchlists`; primary navigation contains the Watchlists route. |
| Watchlist database security boundary remains live | VERIFIED | Fresh Supabase state shows `owner_user_id` remains `NOT NULL`, zero ownerless watchlists, four authenticated owner policies on each watchlist table, no anon grants, and authenticated CRUD-only grants. Two permanent Auth users now exist from real email sign-in attempts; no watchlists or watchlist items have been persisted. |
| Browser client uses only publishable configuration and persistent sessions | VERIFIED | Fresh `lib/supabase-browser.ts` uses only `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` with `persistSession`, refresh and URL-session detection enabled. |
| Application requests the correct production email return URL | VERIFIED | Fresh `components/WatchlistsClient.tsx` sets `emailRedirectTo` to `${window.location.origin}/watchlists`. Supabase API logs show production browser OTP requests with `redirect_to=https://discoverbouldersmarkets.vercel.app/watchlists`, returning HTTP 200. |
| Supabase Auth accepts and returns to the production watchlist URL | **FAILED** | Subsequent real `/auth/v1/verify` requests in Supabase logs use `redirect_to=http://localhost:3000`, with successful signup/login events returning there rather than the requested production `/watchlists` route. This is direct evidence that the project's Auth URL/redirect configuration is falling back to localhost. |
| Real authenticated browser CRUD is end-to-end usable in production | **FAILED / NOT CLOSABLE** | The required production auth return is broken, so a permanent user cannot be considered to have completed the intended production session-return path. Database CRUD/RLS remains independently proven, but the production browser flow is not complete. |
| Existing production runtime remains healthy | VERIFIED | Vercel reports no runtime errors and no 4xx runtime logs in the inspected one-hour production window. |

### Re-review decision

**REWORK.** The prior deployment-only blocker has cleared, and the reviewed frontend now builds and serves successfully. The remaining defect is an actual production configuration failure inside the MON-002 end-to-end contract: the application correctly asks Supabase Auth to return magic-link users to the production `/watchlists` URL, but Supabase verification falls back to `http://localhost:3000`.

This prevents independent verification that real user-owned lists can be maintained through the production application. It is therefore not an external deployment wait and must return to the Builder for remediation.

### Required remediation and closure conditions

1. Correct the Supabase Auth production URL configuration so the production origin and `/watchlists` return URL requested by the application are accepted rather than replaced by `http://localhost:3000`. Use the supported Supabase Site URL / Redirect URLs configuration; do not weaken the RLS model or add privileged browser credentials.
2. Re-run a production email sign-in from `/watchlists` and verify primary Auth/API logs show the verification redirect returning to `https://discoverbouldersmarkets.vercel.app/watchlists` (or an equivalently approved production callback that establishes the same session), not localhost.
3. Verify the returned browser session is a permanent authenticated user and survives the redirect/session-detection path.
4. Exercise the production watchlist path with that permanent session: create a list, edit its metadata, choose/default a list, add an active tracked instrument, update notes, reorder/remove an item, and delete a list. Confirm persisted rows remain owned by that user and no cross-user access is introduced.
5. Reconfirm signed-out public routes remain healthy and the frontend still contains no privileged Supabase credential.
6. Return the same MON-002 item to **IN REVIEW** for targeted independent closure. Do not start MON-003.

### Resulting project-plan status

- MON-002 → **IN PROGRESS** for the exact production Auth redirect/session remediation above.
- MON-003 and all later items remain **PLANNED**.
- No next task is promoted.
