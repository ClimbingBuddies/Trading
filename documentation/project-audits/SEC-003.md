# SEC-003 independent audit

## Review — 20 August 2026, 11:00 AWST

- **Task:** `SEC-003 — Harden helper-function search paths`
- **Project-plan status at review start:** `IN REVIEW`
- **Decision:** `PASS`
- **Final project-plan status:** `DONE`
- **Next promoted task:** `SEC-004 — Review \`pg_net\` warning`

## Definition of Done checks

| Requirement | Verdict | Independent evidence |
|---|---|---|
| Relevant functions use explicit safe search paths | **VERIFIED** | The live `pg_proc` catalog contains exactly the nine application-owned helpers documented by SEC-003. Every one has `proconfig = {search_path=\"\"}`, and `pg_get_functiondef` renders `SET search_path TO ''`. The live applied-migration list includes `20260820023236_harden_helper_function_search_paths`. |
| Application objects remain resolvable with the fixed path | **VERIFIED** | Independent inspection of all nine live function definitions found every application relation and row-type reference explicitly qualified with `public.`; remaining unqualified calls and types are PostgreSQL built-ins resolved from `pg_catalog` or definition-time object OIDs. |
| Hardening preserves the required function boundary and trigger bindings | **VERIFIED** | The seven orchestration helpers remain non-executable by `anon` and `authenticated` and executable by `service_role`. The two intended `SECURITY DEFINER` flags remain on `prepare_chatgpt_market_assessment` and `finalize_chatgpt_market_assessment`. Both trigger helpers remain attached: the independence trigger on `gpt_market_assessments` and ten `updated_at` triggers. |
| Hardened helpers still execute safely | **VERIFIED** | A completed-run call to `prepare_chatgpt_market_assessment` returned queue 15/run `56841c82-321d-440e-98dc-ec73529a432b` as `succeeded`, 30/30, and `already_complete = true`. A no-match queue claim returned no rows. Five independent negative-path and rollback-contained trigger probes passed, covering both finalizers, attempt handling, independence metadata normalization, and `updated_at` behavior without retaining test mutations. |

## Additional verification

- The repository migration `supabase/migrations/20260820023000_harden_helper_function_search_paths.sql` contains one fixed empty-path alteration for each of the nine live helpers.
- The fresh Supabase Security Advisor result contains no `function_search_path_mutable` finding.
- The existing `pg_net` extension-in-public warning is outside SEC-003 and is the explicit subject of newly promoted SEC-004: https://supabase.com/docs/guides/database/database-linter?lint=0014_extension_in_public
- Existing informational `rls_enabled_no_policy` notices are unrelated to the helper-function search-path Definition of Done.
- Vercel evidence was not required because SEC-003 changes database function resolution only and introduces no frontend or deployment change.

## Primary evidence inspected

### GitHub

- `automation/project-plan-auditor.md`
- `documentation/project-plan.md`
- `supabase/migrations/20260820023000_harden_helper_function_search_paths.sql`
- `documentation/security/helper-function-search-path-hardening.md`
- Prior audit record check: `documentation/project-audits/SEC-003.md` did not yet exist.

### Supabase project `glvbqcplgjdfgjyknzsa`

- Live `pg_proc` configuration, attributes, signatures, and full definitions for all nine helpers.
- Applied migration history.
- Function execution privileges for `anon`, `authenticated`, and `service_role`.
- Current trigger bindings for both trigger helpers.
- Idempotent completed-run and no-match queue smoke checks.
- Rollback-contained negative-path and trigger behavior probes.
- Fresh Security Advisor findings.

## Decision rationale

Every material Definition of Done requirement is independently verified from current repository source and live database evidence. The helpers use a fixed empty search path, all application references are fully qualified, and representative behavior plus access boundaries remain intact. No SEC-003 corrective action remains, so the item passes without qualification.
