# SEC-001 — Define public/private Market Assessment access

## Independent review — 19 August 2026, 12:58 Australia/Perth

- **Task:** `SEC-001 — Define public/private Market Assessment access`
- **Project-plan status at review start:** `IN REVIEW`
- **Decision:** `PASS WITH ADVICE`
- **Final project-plan status:** `DONE`
- **Next promoted task:** `SEC-002 — Apply deliberate RLS policies`

## Definition of Done checks

| Requirement | Verdict | Independent evidence |
|---|---|---|
| Published Market Assessment output is explicitly classified | **VERIFIED** | The canonical classification limits public output to assessments from terminal `succeeded` or `partial`, non-test runs; linked evidence is public only through that published assessment set. It lists every approved assessment and evidence field. |
| Public run information is separated from full run control | **VERIFIED** | The document defines a minimal published run envelope and explicitly classifies direct access to full `gpt_market_runs`, test runs, non-terminal state, model/prompt metadata, analysis mode and notes as internal. |
| Queue and scheduler access is explicitly classified | **VERIFIED** | `market_assessment_queue` and `market_assessment_schedule_log`, including status, attempts, run links, errors and notes, are explicitly internal-only. |
| Client write and orchestration boundaries are explicit | **VERIFIED** | The role matrix denies browser/client writes and orchestration-function execution to `anon` and `authenticated`, reserving them for a trusted backend/`service_role`. Authentication alone is correctly not treated as administrative authorization. |
| Classification supports the actual public frontend | **VERIFIED** | Current source uses a publishable Supabase key. `lib/dashboard.ts` reads terminal runs, removes test runs in application code, then reads the selected run's assessments and linked evidence. The approved public envelope contains the fields required to preserve those public routes when `SEC-002` replaces broad table reads. |
| Current live access is truthfully distinguished from the approved target | **VERIFIED** | Live Supabase has RLS enabled on all five tables, read-only client grants on the three GPT tables, no client writes, and no client access to queue/schedule tables. The classification correctly records that current table-wide reads are broader than the target and that two legacy scheduler functions remain client-executable. |
| The decision is discoverable and actionable for the next task | **VERIFIED** | The classification is linked from `documentation/README.md`, reconciled into `documentation/security-and-operational-notes.md`, and includes a nine-point `SEC-002` implementation/verification contract. |

## Primary evidence inspected

### GitHub

- `automation/project-plan-auditor.md`
- `documentation/project-plan.md`
- `documentation/security/market-assessment-access-classification.md`
- `documentation/README.md`
- `documentation/security-and-operational-notes.md`
- `lib/dashboard.ts`
- `lib/supabase.ts`
- `supabase/migrations/20260814050748_secure_market_assessment_rls.sql`
- `supabase/migrations/20260812_phase2_assessment_queue_lifecycle.sql`
- `documentation/project-audits/SEC-001.md` was checked and did not previously exist.

### Supabase

Project `glvbqcplgjdfgjyknzsa` was queried directly for:

- live columns on all five Market Assessment output/control tables;
- RLS enablement and policies;
- effective `anon` and `authenticated` SELECT/INSERT/UPDATE/DELETE privileges;
- effective execution privileges on seven Market orchestration functions;
- current production/test population split;
- current Security Advisor findings.

Fresh live counts establish that the classification is material rather than theoretical:

- 3 total runs, of which 1 is currently terminal and non-test;
- 90 total assessments, of which 30 belong to that published run;
- 158 total evidence rows, of which 68 link to published assessments.

### Vercel / production / browser

Not required for this Definition of Done because SEC-001 defines the access contract and intentionally makes no deployed policy or frontend change. Production route verification is explicitly required by the classification during `SEC-002`, when the access implementation changes.

## Decision

**PASS WITH ADVICE**

The public/private Market Assessment boundary is explicit, field-level, role-aware, consistent with the actual consumer, and clear enough to implement and audit. The document does not misrepresent the broader current policy as already compliant.

## Non-blocking advice

1. During `SEC-002`, implement the published-run boundary with a positive allowlist of approved production modes rather than only a negative `analysis_mode <> 'test'` check. Null, unknown, and newly introduced modes should remain private by default.
2. `documentation/security-and-operational-notes.md` still says the Supabase Security Advisor reports mutable-search-path warnings for two legacy functions. The fresh 19 August 2026 Advisor result did not include that lint, and the live functions currently have `search_path=public`. `SEC-003` should independently decide whether further hardening is still warranted and reconcile the note with current evidence rather than relying on the historical warning.
