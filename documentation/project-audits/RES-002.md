# RES-002 — Operationalise approved opinion sources

## Independent review — 24 August 2026, 14:56 AWST

**Project-plan status at review start:** `IN REVIEW`  
**Decision:** `PASS WITH ADVICE`

### Definition of Done

Project-plan requirement: **Collection, provenance, deduplication and consensus are automated and monitored.**

| Check | Verdict | Independent evidence |
|---|---|---|
| Approved-source collection is automated under an explicit controller | **VERIFIED** | The enabled `External Opinion Review` task runs Monday–Friday at 5:00 pm `America/New_York`, before the 6:15 pm Market Assessment, and fresh-reads `automation/daily-external-opinion-review.md` each run. The runner reads the live `opinion_sources` registry rather than hard-coding approval. |
| Live source registry is explicit and source-specific | **VERIFIED** | Supabase independently shows five active source families with collection method, approved-domain boundary, lookback, per-instrument cap and trust tier: analyst consensus, financial news, official company, regulatory and research. |
| Trusted persistence boundary is deliberate | **VERIFIED** | All six opinion/control tables have zero `anon`/`authenticated` table grants and explicit deny RLS policies. Every function in schema `external_opinion` is non-executable by `anon` and `authenticated` and executable by `service_role`. The former broad client privileges, including `TRUNCATE`, are absent. |
| Canonical provenance and identity are implemented | **VERIFIED** | Live `instrument_opinions` includes canonical source URL/domain/key, claim hash, deterministic observation key and collector version. `gpt_market_evidence` includes `instrument_opinion_id` and `canonical_source_key`. Required live indexes include deterministic observation uniqueness and per-assessment canonical external-source uniqueness. |
| Exact retry ingestion is idempotent | **VERIFIED** | Independent rollback-only audit inserted one NVDA analyst-consensus observation, then retried the same claim using an equivalent URL with tracking removed. First insert returned `inserted=true`; retry returned `inserted=false`, the same opinion identity and the same canonical source key. |
| Consensus is derived from retained atomic evidence with member lineage | **VERIFIED** | Independent rollback finalisation reached `succeeded`; `opinion_consensus_members` contained the retained atomic opinion. Live 24 August consensus has AMD with one member/source family and NVDA with three members across three source families. |
| Same-source Market evidence cannot be counted twice | **VERIFIED** | Independent rollback test linked a real NVDA opinion into Market evidence and verified that a second representation of the same canonical source for the same assessment is rejected by uniqueness. |
| Cross-instrument opinion lineage is rejected | **VERIFIED** | Independent rollback test attempted to attach an NVDA opinion to an AMD Market Assessment and received `External opinion lineage instrument mismatch`. |
| Per-source monitoring and terminal lifecycle are persisted | **VERIFIED** | Live 24 August review has five terminal `succeeded` source results, zero source failures, persisted seen/inserted/duplicate counts and completion timestamps. The research family correctly records a successful zero-insert result rather than fabricating neutral evidence. |
| Coverage monitoring distinguishes current/stale/none | **VERIFIED** | Live `external_opinion_coverage_v1` reports 2 `current`, 0 `stale`, 28 `none`; the review row persists the same counts. `none` is not represented as neutral consensus. |
| A real end-to-end current review completed | **VERIFIED** | Review `a6f2d0dd-bebe-4acf-94d1-c7406a069be5` for New York date 24 August 2026 is `succeeded`, attempt 1, 30 instruments checked, 5 source families checked, 4 opinions seen/inserted, 0 duplicate skips, 2 consensus rows and no error. |
| Same-date completed retry does not create another review | **VERIFIED** | Auditor independently called `external_opinion.prepare_review_v1(2026-08-24,'scheduled-external-opinion-review')`; it returned the same review ID, `succeeded`, `already_complete=true`, attempt count 1. |
| Persisted source evidence has real attributable provenance | **VERIFIED** | Live rows retain SEC, NVIDIA Investor Relations, Reuters and StockAnalysis URLs/references. The Auditor independently opened AMD's 4 August 2026 SEC Form 8-K and NVIDIA's official 26 August FY27 Q2 event page, found the Reuters 22 August price-hike report, and independently confirmed StockAnalysis' 20 August snapshot of 62 analysts / Strong Buy / $304.73. |
| External opinion remains analytically separate from Technical, Market Convergence and Opportunity outputs | **VERIFIED** | Fresh inspection of every live `external_opinion` function found no references to `technical_indicators`, `market_scores`, `market_convergence_assessments`, `opportunity_assessments`, `structural_opportunity_signals` or `technology_inflection_signals`; there are no external-opinion triggers on those output tables. |
| Live database matches the committed migration set | **VERIFIED** | Supabase migration history contains `operationalise_external_opinion_v1`, `fix_external_opinion_function_ambiguities`, `fix_external_opinion_consensus_ambiguities` and `index_external_opinion_foreign_keys`. Live columns/functions/policies/indexes match the operational contract. |
| Implementation is documented | **VERIFIED** | `documentation/pipelines/external-opinion-pipeline.md` documents scheduling, approved sources, trusted persistence, idempotency, consensus/member lineage, evidence lineage, coverage and verification. |
| Production repository state builds cleanly without a frontend privileged secret | **VERIFIED** | RES-002 implementation deployment `dpl_9xL3dVGDoza4NfCCQqbmBPc6dcxm` is `READY` on commit `e90383c6a8e1fa6e2dcbf73877301a93b82b3827`; Vercel build logs show palette check, Next.js compile and TypeScript all passed. Repository search returned no `SUPABASE_SERVICE_ROLE_KEY`. Current production is later documentation/controller-only commit `e0e566e8f3299cef666f4e30ef94a462b4f5ecd9`, also `READY`. |

### Primary evidence inspected

#### GitHub

- `automation/project-plan-auditor.md` — Auditor specification v1.1.
- `documentation/project-plan.md` — exactly one `IN REVIEW` item: RES-002.
- `documentation/specifications/external-opinion-model.md` — audited `external-opinion-v1` model contract.
- `automation/daily-external-opinion-review.md` — canonical scheduled collector specification v1.0.
- `documentation/pipelines/external-opinion-pipeline.md` — implementation/operations documentation.
- `supabase/migrations/20260824143000_operationalise_external_opinion_v1.sql` and the three follow-up RES-002 migrations.
- Repository search for `SUPABASE_SERVICE_ROLE_KEY` returned no matches.
- `documentation/project-audits/RES-002.md` did not exist before this review.

#### Supabase

Project `glvbqcplgjdfgjyknzsa` was queried independently for:

- live RES-002 migration history;
- opinion/source registry configuration;
- opinion, consensus, review, source-result and Market-evidence schema;
- RLS policies, client grants and function execute privileges;
- canonical identity/idempotency indexes and foreign-key indexes;
- persisted 24 August review, source results, atomic opinions, consensus/member counts and coverage;
- same-date completed retry behavior;
- independent rollback-only ingestion/deduplication/finalisation/consensus-member tests;
- independent rollback-only same-source Market-evidence and cross-instrument-lineage enforcement tests;
- live function definitions for prohibited Technical/Convergence/Opportunity dependencies.

No rollback-only audit rows were retained.

#### External source verification

- AMD SEC Form 8-K dated 4 August 2026 was independently opened and confirmed as an AMD second-quarter results filing.
- NVIDIA Investor Relations independently confirms the Q2 FY2027 financial-results event for 26 August 2026.
- Reuters search independently found the 22 August 2026 report used by the stored NVDA financial-news observation.
- StockAnalysis independently confirms the 20 August 2026 analyst snapshot used by the stored NVDA analyst-consensus observation: 62 analysts, Strong Buy, average target $304.73.

#### Scheduler / production

- The active task list was inspected and confirms `External Opinion Review` is enabled on the declared weekday 5:00 pm New York schedule and fresh-reads the GitHub runner.
- The schedule's `last_run_time` is currently null; the verified 24 August production-contract run was executed during implementation rather than by the first unattended scheduled invocation.
- Vercel deployment `dpl_9xL3dVGDoza4NfCCQqbmBPc6dcxm` was independently inspected as `READY`; its build logs passed palette, compilation and TypeScript.
- Current later production commit `e0e566e8f3299cef666f4e30ef94a462b4f5ecd9` is also `READY` and contains controller/documentation follow-up only.

### Non-blocking advice

The new recurring `External Opinion Review` schedule is enabled but has not yet had its first unattended invocation (`last_run_time` is null). The exact production contract has already completed a real end-to-end review successfully, and scheduler ownership/monitoring is live, so this does not fail RES-002. Verify the first unattended scheduled run reaches a terminal review/source state and preserves the same idempotency/coverage behavior; treat any scheduler-only failure as operational follow-up rather than silently assuming the schedule executed.

### Decision

**PASS WITH ADVICE.** Every material RES-002 Definition-of-Done requirement is independently verified from primary evidence. Collection ownership is automated, provenance and canonical identity are enforced, duplicate evidence is prevented, consensus/member lineage is deterministic, monitoring is persisted, the trusted access boundary is deliberate, and a real end-to-end current review has completed.

**Final project-plan status:** `DONE`  
**Next promoted task:** `STRAT-001 — Define first real strategy`
