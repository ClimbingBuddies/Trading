# SEC-002 independent audit

## Review — 20 August 2026, 10:01 AWST

- **Task:** `SEC-002 — Apply deliberate RLS policies`
- **Project-plan status at review start:** `IN REVIEW`
- **Decision:** `PASS`
- **Final project-plan status:** `DONE`
- **Next promoted task:** `SEC-003 — Harden helper-function search paths`

## Definition of Done checks

| Requirement | Verdict | Independent evidence |
|---|---|---|
| Approved dashboard reads continue | **VERIFIED** | The current Vercel production deployment for `boulders-market` was `READY` on the current GitHub handoff commit. Fresh requests to `/assessments`, `/assessments/NVDA`, and `/markets/NVDA` each returned HTTP 200, rendered assessment/NVDA content, and did not render no-data or server-error fallbacks. Current `lib/dashboard.ts` requests only the approved seven-field run envelope. |
| Anonymous writes are blocked | **VERIFIED** | All five Market Assessment output/control tables have RLS enabled. Catalog privilege checks showed no `INSERT`, `UPDATE`, or `DELETE` privilege for `anon` or `authenticated`. A direct zero-row `INSERT` attempted after `SET LOCAL ROLE anon` failed with PostgreSQL `42501 permission denied for table gpt_market_runs`. |
| Internal control tables are protected | **VERIFIED** | `market_assessment_queue` and `market_assessment_schedule_log` have no client table privileges and explicit deny-all client RLS policies. Direct anonymous reads of both tables failed with PostgreSQL `42501`. All seven Market Assessment orchestration functions are non-executable by `anon` and `authenticated`, while `service_role` retains execution. |

## Additional access-boundary verification

- Live Supabase migration history includes `apply_deliberate_market_assessment_rls`, the temporary compatibility migration, and the final `sec002_remove_legacy_frontend_compatibility` migration.
- `anon` and `authenticated` each have column-level `SELECT` on exactly:
  - `run_id`
  - `started_at`
  - `completed_at`
  - `status`
  - `analysis_cutoff_time`
  - `tickers_requested`
  - `tickers_completed`
- Direct reads of `model_name`, `prompt_version`, `analysis_mode`, and `notes` failed as both client roles with PostgreSQL `42501`.
- The database currently contains 4 runs, 120 assessments, and 218 evidence rows. Both client roles see only 2 publishable terminal scheduled runs, 60 linked assessments, and 128 linked evidence rows.
- Neither client role saw a non-terminal or otherwise invalid run.
- The active RLS policies positively constrain published runs, derive assessment/evidence visibility from the published set, and deny client access to queue/schedule state.
- The fresh Supabase Security Advisor result contained no finding against the SEC-002 tables or policies. Its existing `pg_net` extension warning remains separately planned as `SEC-004`: https://supabase.com/docs/guides/database/database-linter?lint=0014_extension_in_public

## Primary evidence inspected

### GitHub

- `automation/project-plan-auditor.md`
- `documentation/project-plan.md`
- `documentation/security/market-assessment-access-classification.md`
- `supabase/migrations/20260819055000_apply_deliberate_market_assessment_rls.sql`
- `supabase/migrations/20260819062500_sec002_legacy_frontend_compatibility.sql`
- `supabase/migrations/20260820013500_sec002_remove_legacy_frontend_compatibility.sql`
- `lib/dashboard.ts`
- `documentation/operations/sec002-market-assessment-rls-deployment-blocker.md`
- Prior audit record check: `documentation/project-audits/SEC-002.md` did not yet exist.

### Supabase project `glvbqcplgjdfgjyknzsa`

- Applied migration history.
- Live RLS-enable flags and current policies for all five Market Assessment output/control tables.
- Live table and column privileges for `anon` and `authenticated`.
- Role-scoped visible counts for runs, assessments, and evidence.
- Direct denial tests for internal columns, both control tables, and an anonymous write attempt.
- Function execution privileges for all seven orchestration functions and `service_role`.
- Fresh Security Advisor findings.

### Vercel / production

- Vercel project `boulders-market` and its current `READY` production deployment.
- Fresh production fetches of:
  - `https://discoverbouldersmarkets.vercel.app/assessments`
  - `https://discoverbouldersmarkets.vercel.app/assessments/NVDA`
  - `https://discoverbouldersmarkets.vercel.app/markets/NVDA`

## Decision rationale

Every material Definition of Done requirement is independently verified from current source, live database privileges/policies and role behaviour, and deployed production behaviour. No SEC-002 corrective action remains, so the item passes without qualification.
