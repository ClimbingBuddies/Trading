# OPS-006 — Verify Market retry/idempotency

## Review — 19 August 2026, 11:02 Australia/Perth

- **Task:** `OPS-006 — Verify Market retry/idempotency`
- **Project-plan status at review start:** `IN REVIEW`
- **Decision:** **PASS WITH ADVICE**
- **Final project-plan status:** `DONE`
- **Next promoted task:** `OPS-007 — Resolve historical Market run and backlog`

## Definition of Done checks

| Requirement | Verdict | Independent verification |
|---|---|---|
| Same-date retry reuses the existing queue and run | **VERIFIED** | A fresh direct call to `prepare_chatgpt_market_assessment('2026-08-18', ...)` returned queue `9` and run `ccb2b535-a454-4988-ad03-1f7f14907428`, with `already_complete = true`. Only one queue exists for the date/process and only one target run exists. |
| Completed retry does not create duplicate assessments | **VERIFIED** | Before and after the independent retry, the target run contained exactly 30 assessment rows across 30 distinct instruments. Duplicate `(run_id, instrument_id)` groups remained 0, and the live database retains a unique constraint on those columns. |
| Completed retry does not create duplicate evidence | **VERIFIED** | Evidence remained exactly 68 rows. Duplicate groups by assessment, source URL and evidence text remained 0. |
| Completed retry is non-mutating | **VERIFIED** | Run/queue statuses remained `succeeded`; attempt count remained 1; run start/completion and queue processed/updated timestamps remained unchanged at their original persisted values. |
| Non-terminal resume reuses existing identifiers | **VERIFIED** | In an independently executed rollback-only probe, a temporary `partial` representation returned the same queue `9` and run ID, `already_complete = false`, run `running`, queue `processing`, and attempt count 2 within the probe. No second queue or run was created. |
| Resume processes no already-complete instruments and creates no duplicates | **VERIFIED** | During the resume probe, 30 assessments across 30 instruments already existed and active instruments missing an assessment were 0. Assessment and evidence counts remained 30 and 68; both duplicate-group counts remained 0. |
| Reused run finalises truthfully | **VERIFIED** | The independent probe called `finalize_chatgpt_market_assessment` on the reused identifiers. It returned `succeeded` with 30 requested and 30 completed; run/queue returned to terminal `succeeded` state with completion timestamps populated. |
| Verification does not leave production test state | **VERIFIED** | The resume/finalise probe ran inside a controlled PL/pgSQL subtransaction and deliberately rolled back. The post-probe production snapshot exactly matched the pre-probe snapshot, including statuses, timestamps, attempt count, assessment rows and evidence rows. |
| Live implementation matches the GitHub migration and preserves helper access controls | **VERIFIED** | Live function text contains the qualified completed-run lookup and resume update introduced by the migration. Migration history contains `fix_market_assessment_prepare_retry`. The function remains `SECURITY DEFINER`, has `search_path=public`, denies execute to `PUBLIC`, `anon` and `authenticated`, and grants execute to `service_role`. |

## Primary evidence inspected

### GitHub

- `automation/project-plan-auditor.md` — Auditor specification version 1.0, blob `50e4ca1d2b7f57505e98418322e6d831b062dfd0`.
- `documentation/project-plan.md` — project state at review start, blob `106a765cc32abbc3bf6ceef3ae228b11aade77de`.
- `automation/daily-market-assessment.md` — canonical retry/resume rules, blob `06b39f10cd0e18d02c226fb8b29fccaec528a985`.
- `supabase/migrations/20260812_phase2_chatgpt_task_helpers.sql` — original orchestration helper, blob `c94f74598e2f1d6dd2411ccd07174ae2aa253698`.
- `supabase/migrations/20260819024000_fix_market_assessment_prepare_retry.sql` — qualified retry/resume fix, blob `d932f708636d9de85c8b57cf653e24824fc8941f`.
- `documentation/operations/market-assessment-retry-idempotency-verification.md` — Builder handoff evidence, blob `b2dd673a69996082baf76481728c8b270c866076`; its claims were re-tested independently.
- No earlier `documentation/project-audits/OPS-006.md` existed.

### Supabase

Read-only inspection and controlled execution against project `glvbqcplgjdfgjyknzsa` covered:

- the live definition and privileges of `public.prepare_chatgpt_market_assessment`;
- the live `public.finalize_chatgpt_market_assessment` execution path;
- `supabase_migrations.schema_migrations`;
- `public.market_assessment_queue`;
- `public.gpt_market_runs`;
- `public.gpt_market_assessments`;
- `public.gpt_market_evidence`;
- `public.instruments`;
- the live unique constraint on `gpt_market_assessments(run_id, instrument_id)`.

The completed-date retry was executed directly. The non-terminal resume and finalisation were exercised within a rollback-only subtransaction so no production state was retained.

### Vercel / production browser

Not required. OPS-006 changes and verifies Supabase orchestration/idempotency and has no frontend or deployment Definition of Done.

## Decision rationale

Every material OPS-006 requirement is independently verified. Both completed and resumable same-date invocations reuse the existing queue/run, avoid assessment and evidence duplicates, maintain persisted progress accounting, and reach truthful terminal state. The corrective migration is present in GitHub and applied live.

## Non-blocking advice

Add repeatable automated database regression tests for the already-complete and resumable branches, including assertions on identifiers, lifecycle transitions, counts, duplicate groups, and permissions. This aligns naturally with the later `QUAL-001` task. The operational checks passed, so the absence of an automated test is not a blocker for OPS-006.
