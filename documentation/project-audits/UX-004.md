# UX-004 Project Audit

## Review — 23 Aug 2026 12:47 AWST

**Task:** UX-004 — Maintain palette compliance  
**Project-plan status at review start:** IN REVIEW  
**Reviewed implementation/documentation state:** `42b0f22ada2ff3f0084f12fee02b6ebace4ed7f3`  
**Current production/controller commit:** `e7b36f93e826b5a35f00cef0454fab9d1336de62`  
**Decision:** PASS WITH ADVICE

### Definition of Done

Definition of Done: **New components use semantic theme/chart tokens rather than fixed page colours.**

| Check | Verdict | Independent evidence |
|---|---|---|
| Shared/new component colour styling uses semantic theme or chart tokens | VERIFIED | Current source for `app/opportunity-exposure-inspector.css`, `app/opportunities/opportunity-daily-status.module.css` and `components/PriceHistoryChart.module.css` uses `--theme-*`, `--chart-*` and `color-mix(...)` derived from those tokens for backgrounds, borders, text, state colours and chart strokes/fills. `components/OpportunityCarousel.tsx` no longer carries the former fixed inline card-footer colour and uses the semantic `cardFooterAction` class. |
| Shared chart components use palette chart tokens | VERIFIED | Current chart components including `OpportunityHistoryChart`, `PriceHistoryChart`, `AssessmentDonut` and `LoadChart` consume `--chart-1` through `--chart-6` / `--chart-grid`; `app/theme-compliance.css` defines those chart tokens for all five selectable palettes. |
| Five supported palettes resolve through the same semantic contract | VERIFIED | `ThemePaletteSelector.tsx` exposes Midnight Blue, Original Green, Copper Ember, Plum Night and Stone Paper. `app/theme.css`, `app/theme-v2.css` and `app/theme-light.css` define the corresponding semantic `--theme-*` values; `app/theme-compliance.css` defines palette-specific chart tokens. |
| Legacy Opportunity styling is behind a semantic compatibility boundary | VERIFIED | `app/theme.css` maps Opportunity-local `--opp-*` variables to global `--theme-*` variables under `html[data-theme] main [class*='opportunityPage']` and also applies semantic surfaces/borders to key Opportunity cards, rows, controls and actions. This means legacy defaults in `opportunities.module.css` do not make the current Opportunity surface a fixed-colour island. |
| Build-time regression guard is real and active | VERIFIED | `package.json` runs `npm run check:palette` in `prebuild`. `scripts/check-palette-compliance.mjs` scans all JS/TS/CSS component files plus the current app-level mobile/Opportunity interaction style files for raw hex/rgb/hsl colour literals and exits non-zero on violations. Current production deployment `dpl_4SCDJF1eGBa9pajbDF4XWX9TnMM8` independently ran the guard and reported `Palette compliance check passed for 16 component/style files.` before a successful Next.js and TypeScript build. |
| No later implementation drift exists between reviewed state and production controller commit | VERIFIED | GitHub comparison `42b0f22ada2ff3f0084f12fee02b6ebace4ed7f3...e7b36f93e826b5a35f00cef0454fab9d1336de62` shows only `documentation/project-plan.md` changed after the reviewed implementation/documentation state. Current production is READY on the controller commit. |
| Representative production routes remain healthy with the palette system present | VERIFIED | `/markets/ANET`, `/opportunities`, and `/opportunities/ai_datacentre_power_cooling?view=exposure` each returned HTTP 200 from current production and expose the five-option palette selector. The Market route contains the tokenised Price History controls; the Opportunity overview contains the tokenised daily-status/card-footer classes; the Exposure route contains the current Opportunity surface and inspector host. |
| Production runtime health | VERIFIED | Vercel production error/fatal query for the preceding hour returned no entries. |
| Fixed named-colour search | VERIFIED | Repository source search for `white` and `black` returned no matches, providing an additional check beyond the guard's raw hex/rgb/hsl pattern. |

### Primary evidence inspected

#### GitHub

- `automation/project-plan-auditor.md` — specification v1.1, retrieved fresh at audit start.
- `documentation/project-plan.md` — retrieved fresh at audit start; UX-004 was the only `IN REVIEW` item.
- `documentation/palette-compliance-review.md` — current palette compliance contract and scope.
- `package.json` — `check:palette` and `prebuild` wiring.
- `scripts/check-palette-compliance.mjs` — actual source guard implementation and failure behaviour.
- `app/opportunity-exposure-inspector.css` — semantic inspector and SVG chart styling.
- `app/opportunities/opportunity-daily-status.module.css` — semantic daily-status and card-footer styling.
- `components/OpportunityCarousel.tsx` — no fixed inline presentation colour; semantic footer class used.
- `components/PriceHistoryChart.module.css` — semantic period-control and chart-progress styling.
- `components/ThemePaletteSelector.tsx` — five supported palettes and runtime theme selection.
- `app/theme.css`, `app/theme-v2.css`, `app/theme-light.css`, `app/theme-compliance.css` — semantic palette definitions, Opportunity compatibility mapping and chart-token definitions.
- GitHub comparison `42b0f22ada2ff3f0084f12fee02b6ebace4ed7f3...e7b36f93e826b5a35f00cef0454fab9d1336de62` — only project-plan controller documentation changed after the reviewed state.
- Repository searches for `white` and `black` — no matches.

#### Supabase

Not applicable to this frontend presentation-only Definition of Done. No database mutation or persisted-data evidence was required for the decision.

#### Vercel / production

- Current production deployment: `dpl_4SCDJF1eGBa9pajbDF4XWX9TnMM8`.
- State: `READY`.
- Production commit: `e7b36f93e826b5a35f00cef0454fab9d1336de62`.
- Production build logs independently show the palette guard passed for 16 component/style files, Next.js compiled successfully and TypeScript completed successfully.
- `/markets/ANET`: HTTP 200; five-palette selector and Price History surface present.
- `/opportunities`: HTTP 200; five-palette selector plus daily-status/card-footer implementation present.
- `/opportunities/ai_datacentre_power_cooling?view=exposure`: HTTP 200; five-palette selector and current Opportunity Exposure surface present.
- Production error/fatal logs for the preceding hour: none.

### Decision

**PASS WITH ADVICE.** The reviewed shared/new frontend components use semantic theme/chart tokens rather than fixed page colours, the legacy Opportunity surface is explicitly bridged into the semantic palette system, and a build-time source guard is active and passing in current production. The Definition of Done is satisfied.

### Non-blocking advice

The current guard automatically scans `components/` but enumerates app-level component styles through an explicit extra-file list. As new app-level CSS modules are introduced, prefer evolving the guard to discover eligible presentation modules by convention or directory rule so a newly created app-level component stylesheet cannot be omitted from the guard accidentally. This does not block UX-004 because the current shared/new UX files are covered and verified.

### Resulting project-plan action

- UX-004 may move to **DONE**.
- MON-001 — Decide watchlist/auth model is the next valid **PLANNED** item and may move to **NEXT**.
- No other project-plan item should be promoted.
