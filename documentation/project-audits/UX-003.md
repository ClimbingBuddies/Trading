# UX-003 Project Audit

## Review — 23 Aug 2026 11:10 AWST

**Task:** UX-003 — Complete mobile interaction review  
**Project-plan status at review start:** IN REVIEW  
**Reviewed repository state:** `741e1f1fa57b4d70f76013b2bb49c7acaeba869b`  
**Current production/handoff commit:** `38bb1b246e8c8a7d1eb55ca834688d7ea25bec8d`  
**Decision:** REWORK

### Definition of Done

Definition of Done: **Headers, tabs, swipe, touch targets and responsive tables are verified.**

| Check | Verdict | Independent evidence |
|---|---|---|
| Mobile headers reflow without relying on desktop layout | VERIFIED | Current production CSS stacks standard `.pageHeader` at `<= 640px`; Opportunity `.overviewHeader` / `.detailHero` stack at `<= 760px`, with Opportunity detail actions stretched to the available width. Production `/markets/ANET` and `/opportunities/ai_datacentre_power_cooling?view=exposure` both return HTTP 200 with the expected header DOM. |
| Primary navigation and Opportunity tabs remain horizontally swipe/scroll capable | VERIFIED | Deployed CSS makes the primary navigation a horizontal `overflow-x:auto` rail at `<= 900px` and applies touch panning, contained overscroll and proximity scroll snapping. The six Opportunity tabs are a non-wrapping flex rail with `overflow-x:auto`; deployed UX-003 CSS adds touch panning, contained overscroll, proximity snapping and 44px minimum tab height at `<= 760px`. |
| Opportunity overview carousel retains native swipe behaviour | VERIFIED | The deployed Opportunity bundle uses `overflow-x:auto`, `scroll-snap-type:x mandatory` and `overscroll-behavior-x:contain` for the theme carousel; mobile cards use an 88% flex basis so the next card remains discoverable. |
| Mobile touch targets satisfy the documented 44px floor across the reviewed interaction surface | FAILED | The live `/markets/ANET` route includes seven Price History period buttons. Its deployed route CSS explicitly sets `.rangeButton` / `.rangeActive` to `min-height: 32px`, including the `<=720px` mobile layout, and the UX-003 override does not target these controls. This leaves material mobile controls below the documented 44px touch-target floor. |
| Standard responsive tables use deliberate horizontal scrolling rather than unreadable collapse | VERIFIED | Production `/markets/ANET` contains the `.tableScroll` wrapper. Deployed base CSS uses `overflow-x:auto` with nowrap table cells; UX-003 production CSS adds `max-width:100%`, touch panning, momentum scrolling and contained horizontal overscroll at `<=900px`. |
| Opportunity responsive tables preserve a horizontal scroll wrapper | VERIFIED | The deployed Opportunity stylesheet retains `.darkTableScroll { overflow-x:auto; }` and mobile table-cell spacing while other Opportunity layouts reflow at `<=760px`. |
| Reviewed implementation is present in production without later implementation drift | VERIFIED | Vercel production deployment `dpl_Hq77staTknDxVjWWBFLkejdynoxH` is `READY` at commit `38bb1b246e8c8a7d1eb55ca834688d7ea25bec8d`. GitHub comparison from reviewed state `741e1f1...` to production/handoff `38bb1b2...` shows only `documentation/project-plan.md` changed; no UX-003 implementation file changed after review state. |
| Production route/runtime health | VERIFIED | `/markets/ANET`, the production global CSS bundle, the Opportunity exposure route and Opportunity route CSS all returned HTTP 200. Vercel reported no runtime errors in the preceding hour. |

### Primary evidence inspected

#### GitHub

- `automation/project-plan-auditor.md` — specification v1.1, retrieved fresh at run start.
- `documentation/project-plan.md` — retrieved fresh at run start; UX-003 was the only `IN REVIEW` item.
- `documentation/mobile-interaction-review.md` — reviewed mobile contract.
- `app/mobile-interaction.css` — current interaction overrides.
- `app/layout.tsx` — confirms the mobile interaction layer is imported after the existing responsive/theme layers.
- GitHub comparison `741e1f1fa57b4d70f76013b2bb49c7acaeba869b...38bb1b246e8c8a7d1eb55ca834688d7ea25bec8d` — only the project-plan handoff changed after the reviewed repository state.
- No previous `documentation/project-audits/UX-003.md` existed before this review.

#### Supabase

Not applicable to this frontend interaction-only Definition of Done; no database state was changed or used as evidence for the decision.

#### Vercel / production

Project: `boulders-market`.

- Current production deployment: `dpl_Hq77staTknDxVjWWBFLkejdynoxH`.
- State: `READY`.
- Production commit: `38bb1b246e8c8a7d1eb55ca834688d7ea25bec8d`.
- `/markets/ANET`: HTTP 200; expected standard header, primary navigation, detail back link, Price History range controls and `.tableScroll` table are present.
- Global production stylesheet `/_next/static/immutable/chunks/0g3lkmncuxunz.css`: HTTP 200; contains the UX-003 mobile interaction rules and the existing standard responsive breakpoints.
- `/opportunities/ai_datacentre_power_cooling?view=exposure`: HTTP 200; expected Opportunity detail header, theme select and six-tab navigation are present.
- Opportunity production stylesheet `/_next/static/immutable/chunks/161nxcd-1zn_e.css`: HTTP 200; contains the native carousel, tab rail, Opportunity responsive breakpoints and `.darkTableScroll` behaviour.
- Price History production stylesheet `/_next/static/immutable/chunks/26b8lsf5d0ojc.css`: HTTP 200; explicitly sets the seven period buttons to `min-height:32px`, including at `<=720px`.
- Runtime-error query for the preceding hour: no runtime errors found.

### Browser/user-flow limitation

The connected production fetch tooling does not expose an interactive emulated mobile viewport, so this review does not claim that a physical touch gesture was executed. The deployed DOM and production CSS are sufficient to prove the material 32px touch-target defect and therefore sufficient for the REWORK decision.

### Decision

**REWORK.** Header, swipe-rail and responsive-table implementation is materially in place, but the touch-target requirement is not complete. The production Market detail page still exposes Price History period controls at a 32px minimum height on mobile, below the documented 44px floor. Because a material Definition of Done condition is `FAILED`, UX-003 cannot advance.

### Required remediation

1. Extend the mobile interaction treatment to the Price History period controls used on `/markets/[symbol]`, so each mobile period control has a minimum touch target of at least 44px while preserving the seven-option responsive layout.
2. Re-review the representative UX-003 routes at a narrow mobile viewport (`/markets/ANET`, `/assessments/anet`, `/opportunities/ai_datacentre_power_cooling?view=exposure`, `/opportunities`) for other material interactive controls below the 44px touch-target floor. At minimum include visible buttons, links styled as actions, tabs, selects, filters/search controls and any client-rendered Opportunity inspector/range controls that are part of those flows.
3. Record repeatable narrow-viewport verification evidence showing: header containment, horizontally scrollable nav/tab/table rails, touch targets at or above 44px, and no unintended page-level horizontal overflow.
4. Return the same UX-003 item to `IN REVIEW`; do not start or promote UX-004.

### Non-blocking advice

A small repeatable viewport interaction test would make future mobile regressions easier to audit, but the Builder may choose the implementation mechanism.

### Resulting project-plan status

- UX-003 → **IN PROGRESS**
- UX-004 remains **PLANNED**
- No next task promoted

---

## Re-audit — 23 Aug 2026 12:21 AWST

**Task:** UX-003 — Complete mobile interaction review  
**Project-plan status at review start:** IN REVIEW  
**Reviewed implementation/documentation state:** `ed190d337bc6a73f365c1be47cb90f130ced3590`  
**Production/controller commit:** `d20c2992ca4837208ad1eee90d919ec62fd0aadb`  
**Decision:** PASS WITH ADVICE

### Definition of Done re-verification

| Check | Verdict | Independent evidence |
|---|---|---|
| Mobile headers reflow correctly | VERIFIED | Deployed global CSS stacks `.pageHeader` at `<=640px`; deployed Opportunity CSS stacks `.overviewHeader` and `.detailHero` at `<=760px`, resets score alignment, stretches detail actions and removes the theme-select minimum width. Representative Market, Assessment and Opportunity routes all returned HTTP 200 with the expected header DOM. |
| Primary navigation and Opportunity tabs are swipe/scroll capable | VERIFIED | Production global CSS makes the primary navigation a horizontal `overflow-x:auto` rail at `<=900px` with touch panning, contained overscroll and proximity snapping. Opportunity `.detailTabs` is a flex row with `overflow-x:auto` and child tabs `flex:none`; the UX-003 mobile layer adds touch panning, contained overscroll, proximity snapping and 44px tab targets. |
| Opportunity overview swipe behaviour remains valid | VERIFIED | The deployed Opportunity stylesheet defines the carousel track with `overflow-x:auto`, `scroll-snap-type:x mandatory` and `overscroll-behavior-x:contain`; mobile theme cards use an 88% basis where that carousel is used. Current `/opportunities` returned HTTP 200 with the Opportunity card interactions present. |
| Material mobile touch targets meet the 44px floor | VERIFIED | The prior failing Price History stylesheet now retains its seven-column mobile grid while setting `.rangeButton` / `.rangeActive` to `min-height:44px` at `<=720px`. Deployed global UX-003 CSS also applies 44px minimums to primary nav, palette select, Opportunity tabs/theme select/action and ticker links, filter/action/search controls, detail back links, and client-rendered Opportunity inspector tabs/ranges/actions. |
| Standard responsive tables scroll deliberately | VERIFIED | `/markets/ANET` contains `.tableScroll`; deployed CSS keeps `overflow-x:auto` and adds `max-width:100%`, touch panning, momentum scrolling and contained horizontal overscroll at `<=900px`. |
| Opportunity responsive tables retain a scroll wrapper | VERIFIED | Current Opportunity production CSS retains `.darkTableScroll { overflow-x:auto; }` and mobile cell spacing. |
| Reviewed implementation matches production | VERIFIED | GitHub comparison `ed190d337bc6a73f365c1be47cb90f130ced3590...d20c2992ca4837208ad1eee90d919ec62fd0aadb` shows only `documentation/project-plan.md` changed after the reviewed implementation state. Production is READY on the controller commit, so the reviewed implementation is present without later implementation drift. |
| Production route/runtime health | VERIFIED | `/markets/ANET`, `/assessments/anet`, `/opportunities/ai_datacentre_power_cooling?view=exposure`, `/opportunities`, and the relevant compiled CSS bundles returned HTTP 200. Production error/fatal log query for the preceding hour returned no entries. |

### Primary evidence inspected

#### GitHub

- `automation/project-plan-auditor.md` — specification v1.1, retrieved fresh at re-audit start.
- `documentation/project-plan.md` — retrieved fresh at re-audit start; UX-003 was the only `IN REVIEW` item.
- `documentation/project-audits/UX-003.md` — prior REWORK and required remediation.
- `components/PriceHistoryChart.module.css` — current mobile Price History rule sets `min-height:44px` at `<=720px` while preserving seven columns.
- `app/mobile-interaction.css` — current mobile target, swipe and table-scroll rules.
- `documentation/mobile-interaction-review.md` — repeatable verification matrix and overflow contract.
- GitHub comparison `ed190d337bc6a73f365c1be47cb90f130ced3590...d20c2992ca4837208ad1eee90d919ec62fd0aadb` — only controller documentation changed after implementation.

#### Supabase

Not applicable to this frontend interaction-only Definition of Done. No Supabase mutation or database evidence was required for this re-audit.

#### Vercel / production

- `boulders-market` production was READY on commit `d20c2992ca4837208ad1eee90d919ec62fd0aadb` during the re-audit.
- `/markets/ANET`: HTTP 200; contains standard header/actions, seven Price History period buttons and `.tableScroll`.
- Current Price History CSS: HTTP 200; mobile period controls are 44px minimum at `<=720px` with the seven-option grid preserved.
- Current global CSS: HTTP 200; contains primary-nav swipe rules, 44px palette/action/select/search/Opportunity inspector target rules, detail-link targets and responsive table containment.
- `/assessments/anet`: HTTP 200 with expected header/detail navigation.
- `/opportunities/ai_datacentre_power_cooling?view=exposure`: HTTP 200 with detail header, theme select, six-tab rail and exposure actions.
- Current Opportunity CSS: HTTP 200; verifies responsive header stacking, tab overflow, carousel swipe semantics and `.darkTableScroll`.
- `/opportunities`: HTTP 200 with current Opportunity overview/card interactions.
- Production error/fatal logs for the preceding hour: none.

### Browser/user-flow limitation and advice

The connected production tooling can verify live DOM, compiled production CSS, deployment identity and runtime health, but it does not provide an interactive emulated mobile viewport that physically performs a swipe or directly measures `scrollWidth` versus viewport width. The deployed rules are deterministic and sufficient to verify this Definition of Done, so this is not blocking.

**Advice:** add a small automated narrow-viewport browser regression test that asserts the representative UX-003 routes have no unintended page-level horizontal overflow, checks 44px computed target heights and exercises the deliberate nav/tab/table horizontal rails. This would make future mobile interaction audits stronger and faster.

### Decision

**PASS WITH ADVICE.** The prior material touch-target failure is fixed, the broader mobile target surface is covered, responsive headers and deliberate horizontal rails are present, and production is healthy. UX-003 satisfies its Definition of Done.

### Resulting project-plan action

- UX-003 may move to **DONE**.
- UX-004 — Maintain palette compliance is the next valid **PLANNED** item and may move to **NEXT**.
- No other project-plan item should be promoted.
